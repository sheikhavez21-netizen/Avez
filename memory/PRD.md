# Relay Premium Car Care — PRD

## Original Problem Statement
"Build a web app chnage the black to white colour keep the papap orange nad chnage the logo and make UX better" — rebuild the existing dark-themed Relay Car Care landing page (single HTML file) as a light/white-themed site, keep the orange accent, use the new uploaded RELAY Premium Car Care logo, and improve UX.

## Product
Landing page for Relay Premium Car Care — doorstep premium car wash service in Goa, India. All bookings via WhatsApp (wa.me/919665980103) or phone (+91 96659 80103).

## User Personas
- Goan car owner who values convenience ("Susegad" lifestyle) and wants doorstep car care
- Owner of ceramic/PPF-protected vehicles needing safe maintenance washes
- Repeat customer interested in monthly subscription plans

## Architecture
- Frontend: React (CRA + craco), Tailwind, shadcn/ui (Accordion), lucide-react icons
- Fonts: Outfit (headings), Manrope (body)
- Backend: FastAPI template retained (no app-specific endpoints needed yet)
- Logo asset: /app/frontend/public/assets/relay-logo.png (whitespace-trimmed)
- Design guidelines: /app/design_guidelines.json

## Core Requirements (static)
- White/light theme, orange (#FF5A00) accent retained
- New RELAY Premium Car Care logo in nav and footer
- Better UX: sticky glass nav, scroll-reveal animations, accordion inclusions, mobile sticky CTA, mobile menu

## Implemented
- 2026-08-23: Full light-theme rebuild in React — Navbar (glass, mobile menu), Hero (headline, CTAs, trust badges, hero image), Philosophy/Susegad section, 4 wash packages with accordion inclusions (₹449–₹1,299), How It Works (3 steps), 4 subscription tiers (₹899–₹3,299) with perks, Why Relay standards, dark contrast Booking CTA section, Coverage (Panjim) + Footer with logo and contacts, mobile sticky booking bar. All CTAs link to WhatsApp with pre-filled messages. data-testid on all interactive elements.
- 2026-08-23 (v2): Monthly plans moved off the home page to dedicated /monthly route (nav "Monthly Plans" pill links there). Relay wash video added to hero (transcoded H.264 mp4 + VP8 webm for browser compatibility, autoplay muted loop). Real photos: frames extracted from the wash video replace all stock images (hero poster, philosophy, why-relay). Logo made transparent (white background removed). New Testimonials section (3 Panjim customer quotes — PLACEHOLDER quotes, need real ones). New FAQ section (6 tap-to-expand questions on products, water/electricity, timing, booking, payment, coverage).
- 2026-08-23 (v3): Slot booking form (id=book-slot) after Packages — name (optional), package select, date picker, 5 time-slot chips, location; validates then opens WhatsApp with pre-filled booking message. Package card buttons pre-select their package in the form via "relay:select-package" event and scroll to it. Hero + mobile sticky CTAs now scroll to the form. Area Checker (id=areas) — locality input with instant yes/coming-soon/not-yet answers against 15 served + 6 coming-soon Goan localities; results link to booking form or WhatsApp waitlist/request.
- 2026-08-23 (v4): Backend: POST /api/bookings (public, saves to MongoDB), GET /api/blocked-slots?date= (public), admin-key-protected (X-Admin-Key, compare_digest) GET /api/admin/bookings, GET/POST/DELETE /api/admin/blocked-slots. Owner dashboard at /admin (key-gated): bookings list + blocked slot manager (whole-day or per-slot, dedupe 409). Booking form disables blocked slot chips live and blocks fully-booked days. Booking form now requires car type (Hatchback/Sedan/Compact SUV/Large SUV) with per-type price estimate shown, and mandatory water + electricity Yes/No — No on either blocks the booking. FAQ water/electricity answer updated to match. Design upgrade: lenis momentum scrolling with anchor interception, framer-motion kinetic hero (masked line-by-line reveal, parallax + mouse-tilt 3D video card), editorial outlined-text marquee, staggered reveals on packages/steps, manifesto chapter numerals restyled. Car-type prices are ESTIMATES pending owner confirmation.
- 2026-08-23 (v5): Merged water + electricity into a single required "Water & electricity available?" Yes/No (utilities_available field) — No blocks the booking. Testimonials/Reviews section removed entirely (business not launched yet, no real reviews) along with its nav link.
- 2026-08-23 (v6): How It Works ("Three steps. Zero hassle.") section and its nav link removed. Booking form now requires a map pin: Leaflet/OpenStreetMap map (no API key) with tap-to-pin, draggable marker, and "Use my current location" (browser geolocation). Pin is saved with the booking (lat/lng) and included in the WhatsApp message as a Google Maps link (https://maps.google.com/?q=lat,lng) — structured for the owner's planned WhatsApp chatbot. Admin dashboard booking rows link to the pinned location.
- 2026-08-23 (v7): Address search added above the map (Nominatim geocoding, Goa-biased, no API key) — customer types their address/area, hits "Find on map", pin drops automatically; tap/drag fine-tuning and current-location button retained.
- 2026-08-23 (v8): Hero copy updated — removed "Doorstep Premium Car Care · Goa" eyebrow; new tagline: "Premium Doorstep Car Wash & Detailing. You stay Susegad. We take care of the car."
- 2026-08-23 (v9): Monthly Plans removed from the site (nav links + /monthly route gone) — to be re-added later; Subscriptions component and MonthlyPlans page files kept on disk for easy restore. Unknown routes now redirect to home.
- 2026-08-23 (v10): All pricing removed from public view — package cards no longer show prices, booking form package dropdown and WhatsApp message no longer include price estimates. Price data retained in content.js for later use.
- 2026-08-23 (v11): Pexa-inspired features added: (1) Launch offer banner (orange strip, code RELAY100, ₹100 off first wash) above a now-sticky navbar; (2) Quick-action tiles (Book a Wash / View Packages / Chat on WhatsApp) + stats bar (2 min booking, 15+ localities, 5 slots daily, 100% doorstep — honest placeholders, update post-launch); (3) Services category grid with real footage images (4 categories mapped to packages, click pre-selects package in booking form and scrolls to it). Razorpay online payment APPROVED by user but PENDING keys — needs RAZORPAY_KEY_ID + RAZORPAY_KEY_SECRET from owner's Razorpay dashboard; playbook ready (server-side order creation in paise, signature verification, price matrix server-side to prevent tampering).
- 2026-08-23 (v12): REVERTED all post-Pexa changes at owner's request — removed offer banner, quick-action tiles, stats bar, services grid, Home nav link; navbar back to fixed overlay. Site is back to the post-pricing-removal state (hero, philosophy, marquee, packages, booking form with map pin + utilities check, why-relay, area checker, FAQ, CTA, footer). Razorpay still pending keys (not implemented).
- 2026-08-24 (v13): Mobile fixes — hero headline switched to fluid text-[11.5vw] on small phones (was clipped at 320px), "Serving Panjim" chip no longer sticks past the screen edge. Verified no horizontal overflow at 320px and 375px.
- 2026-08-24 (v14): Philosophy section rewritten per owner's spec — new manifesto copy for all four points (Susegad-as-emotion, European lab-tested products, "Trained Captains", 2-minute WhatsApp booking), "Just Relay it." emphasized in orange italic, new cinematic visual (clean frame from the wash video) with bold overlay "A PROPER WASH MAKES ALL THE DIFFERENCE.", Relay Promise quote moved below the two-column grid, oversized grey numbers kept (smaller on mobile).
- 2026-08-24 (v15): Philosophy 01–04 numbered points replaced with The Relay Standard (Trained & Verified Captains / Premium-Grade Products / Doorstep On Your Time — orange rule accent style, no numbers). Both section images removed (philosophy visual + why-relay photo); the separate Why Relay section was removed and its content merged into the philosophy section's right column.
- 2026-08-24 (v16): Responsive UX polish for mobile + laptop — mobile menu links enlarged with separators (bigger tap targets), mobile Book Now button full-size, hero top spacing tightened on phones, time-slot chips and Yes/No buttons given larger touch areas. Verified at 375px and 1366px: no overflow anywhere including /admin (login + dashboard stack cleanly).
- 2026-08-24 (v17): Major simplification per owner — REMOVED the entire booking form section ("Pick your slot"), the area checker ("Do we serve your area?"), the FAQ section, and the running marquee banner. All CTAs now open WhatsApp directly (hero, package cards pre-fill package name, service cards pre-fill package name, quick tiles, mobile sticky bar). Backend bookings/blocked-slots APIs and /admin dashboard remain (hold historical bookings) but nothing public writes to them now. Car make/model data (CAR_MAKES) retained in content.js for the future WhatsApp-bot flow. Page now: Hero → Quick Actions → Philosophy (+ Relay Standard) → Services grid → Packages → CTA → Footer.
- 2026-08-24 (v18): Services grid removed per owner. Mobile number + OTP login added in DEMO MODE (owner's choice — no SMS sent; code shown on screen): POST /api/auth/send-otp (stores hashed-style OTP in Mongo with 5-min expiry, single-use), POST /api/auth/verify-otp (issues 30-day JWT), GET /api/auth/me. /login page with +91 phone input, OTP step with demo code box, JWT session in localStorage. Navbar shows Login button or the logged-in number with logout. Verified: full flow incl. wrong-number validation, wrong OTP 401, session persistence across reload, logout. TO GO LIVE: plug Twilio Verify or MSG91 into send-otp/verify-otp (needs owner's provider credentials).
- 2026-08-24 (v19): Make/Model selection added back as a booking popup (Pexa-style): tapping Book on any package card or the hero CTA opens a dialog with Select Make → Select Model (CAR_MAKES data in content.js, 15 makes with popular models + Other), validation, then "Continue on WhatsApp" with message "I want to book {package}. Car: {make} {model}". Custom event "relay:open-booking" drives the modal.
- 2026-08-24 (v20): Full vehicle-selection step before packages per owner's detailed spec — new VehicleSelect section (#select-car): "Let's start with your car." → Select your car (search bar + popular brands grid + compact all-brands grid, 29 brands + Other, monogram cards in Relay style) → Select your model (dynamic per brand) → vehicle type cards (Hatchback/Sedan/SUV/MUV/Coupe/Convertible/Pickup/Other, auto-preselected from model mapping) → confirm card with Change car → "Continue to packages". Progress indicator 01 Car → 02 Package → 03 Slot → 04 Confirm. Data structure VEHICLES = [{brand, models:[{name, vehicleType}]}] in content.js — scalable for per-make/model/type pricing later. Selected vehicle persisted in localStorage (relay_vehicle) and consumed by BookingModal (shows "Your car" row with Change, includes type in WhatsApp message). Guest flow, no login required.
- 2026-08-24 (v21): Vehicle selection moved OFF the landing page into the account per owner — VehicleSelect section removed. New /account page ("Your garage", requires login): name + car make + model (vehicle type auto-derived from model — no separate type question per owner). Backend: GET/PUT /api/profile (JWT-protected). Login now pulls the saved profile and caches the vehicle locally. Package cards show personalized prices when a car is saved ("₹799 for your Creta", via PRICE_TYPE_MAP: Hatchback/Sedan/Coupe/Convertible→base tiers, SUV→Compact SUV, MUV/Pickup→Large SUV) and "Log in & add your car to see your price" hint when not. Nav phone pill links to /account. Prices remain the ESTIMATE matrix in content.js pending owner's real price list.
- 2026-08-24 (v22): Home button added to nav (desktop + mobile menu), scrolls to top.
- 2026-08-24 (v23): Our Story section added between Packages and the booking CTA — approved founder-story narrative verbatim, sticky left column (orange label, "We thought car care could be easier." headline, real Relay footage image), right column story copy with orange-quoted "Your car deserves proper care. Your time deserves better.", closing giant "YOUR DRIVEWAY. OUR PIT CREW." statement with orange divider. Fully responsive (stacks on mobile).
- 2026-08-24 (v24): Story moved off the landing page to a dedicated /about page per owner — "About Us" added to the header nav (desktop + mobile). Story image removed; page is a clean centered reading column with the approved narrative, orange quote bar, and closing "YOUR DRIVEWAY. OUR PIT CREW." statement.
- 2026-08-24 (v25): NEXT ITERATION per owner's full spec. (1) New booking wizard at /book: 5 steps with progress bar (01 Car → 02 Service → 03 Slot → 04 Location → 05 Confirm) — make/model/registration, package cards with per-vehicle pricing, date+slot chips, area+building, summary + payment (Cash / UPI-on-WhatsApp), success screen with RL-XXXXXX booking ID + Track + WhatsApp buttons; booking POSTs to backend with reg_no/payment_method/phone. (2) Track My Pit Stop at /track: RL-ID lookup (?id= supported), booking detail card with technical labels, ETA line, 6-stage pit-crew timeline (Pit Stop Confirmed → Crew Assigned → On The Way → Arrived → In Progress → Complete) with animated current step; current stage derived from booking date (MOCKED until real crew status). (3) My Relay (/account): upcoming + past pit stops with track links, My Vehicle garage form, logout. (4) How It Works restored (3 steps with orange pit-lane progress line). (5) Relay Club teaser section (dark, waitlist CTA, modular). (6) Relay At Your Community section with Palm Residency concept card (marked "Concept preview"). (7) Relay Standard updated to 5 items (crew/products/equipment/process/doorstep). (8) New nav: Packages / How It Works / Our Story / Relay Club / Track My Pit Stop + PANJIM·GOA label + My Relay + Book Now. (9) Packages get technical codes (SPORT-01/GT-02/RS-03/CT-04) and EST. time labels; all Book CTAs now route to /book (hero, nav, cards, mobile sticky, community card). Backend: GET /api/bookings/{id} (public track), GET /api/my/bookings (JWT). BookingModal removed (superseded by wizard).

## Verified
- Page loads, all sections render, package accordion expands, backend /api/ healthy
- v2: subscriptions absent from home, present on /monthly; FAQ accordion expands; hero video plays (verified time advancing); logo transparent on glass nav
- v3: package card click pre-selects "RS Signature Wash" in form; empty submit shows validation error; filled submit opens wa.me with full pre-filled message (verified decoded text); area checker: Miramar → served, Porvorim → coming soon, unknown → not yet (all verified)

## Backlog
- P0: Replace placeholder testimonial quotes with real customer quotes
- P1: More real photos/videos of crew at work, Google Maps coverage area
- P2: Booking form with slot picker (beyond WhatsApp), multilingual (Konkani/English), Instagram/social links

## Next Tasks
1. Replace stock imagery with real Relay photos when provided
2. Add customer testimonials section
3. Add FAQ section
